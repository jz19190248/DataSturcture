#include <iostream>
#define ElemType int
using namespace std;
typedef struct
{
    Node *pointer;
    ElemType value;
} Node;

void SelcetSort(Node *link)
{
    Node *p = link->pointer;
    Node *h = link, *p, *q, *r, *s;
    while (h != NULL)
    {
    }
}